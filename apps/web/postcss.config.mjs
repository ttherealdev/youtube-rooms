// Tailwind v4 is a PostCSS plugin; there is no tailwind.config.js. The design
// tokens live in src/styles/app.css under `@theme inline`.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
